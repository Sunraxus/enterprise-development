name: .NET Tests

on:
  push:
    branches: [ main, labs ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore dependencies
        run: dotnet restore ./EstateAgency/EstateAgency.sln

      - name: Build
        run: dotnet build ./EstateAgency/EstateAgency.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test ./EstateAgency/EstateAgency.Test/EstateAgency.Test.csproj --configuration Release --verbosity normal
